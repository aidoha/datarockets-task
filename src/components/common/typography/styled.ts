// import styled from 'styled-components';
import { TypographyProps } from '.';

// export const h1 = styled.h1<Omit<TypographyProps, 'variant'>>`
// 	color: ${({ color }) => color};
// 	text-align: ${({ textAlign }) => textAlign};
// `;

// export const p = styled.p<Omit<TypographyProps, 'variant'>>`
// 	color: ${({ color }) => color};
// 	text-align: ${({ textAlign }) => textAlign};
// `;
